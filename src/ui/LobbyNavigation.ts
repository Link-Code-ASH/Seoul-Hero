export function lobbyReturnButton(): string {
  return `<button type="button" class="lobby-return" data-action="lobby" aria-label="이전 화면으로"><span class="lobby-return-arrow" aria-hidden="true">←</span><span>이전</span></button>`;
}
