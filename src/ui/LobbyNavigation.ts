export function lobbyReturnButton(): string {
  return `<button type="button" class="lobby-return" data-action="lobby" aria-label="로비로 돌아가기"><span class="lobby-return-arrow" aria-hidden="true">←</span><span>로비</span></button>`;
}
