/** Simulation guardrails and tuning; content stats belong in their own data tables. */
export const SIMULATION_CONFIG = {
  hitFlashSeconds: 0.12,
  eliteRadiusMultiplier: 1.5,
  maxDebugEnemies: 1000,
  maxInputDelta: 0.1,
  maxDeveloperTime: 86400,
} as const;
