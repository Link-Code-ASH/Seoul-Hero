# Casual blessing art

The seven `blessing_*_casual_01.png` masters were generated during the interrupted Seoul Hero casual-art redesign as distinct, transparent Dangun-blessing artifacts. They contain no baked labels. `scripts/optimize-casual-art.py` produces their WebP runtime copies; `src/data/images.ts` is the shared image manifest.
